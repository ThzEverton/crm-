import { z } from 'zod'

export const appointmentSchema = z.object({ patientId: z.string().uuid(), date: z.string().min(1), time: z.string().regex(/^\d{2}:\d{2}$/), durationMinutes: z.coerce.number().int().min(15).max(240), mode: z.enum(['online','in_person']), type: z.enum(['Primeira consulta','Retorno','Avaliação','Consulta de acompanhamento']), notes: z.string().trim().max(2_000).default(''), onlineLink: z.string().trim().max(500).default(''), reminderEnabled: z.string().optional().transform(Boolean), reminderHours: z.coerce.number().int().min(1).max(168).default(24), recurrence: z.enum(['none','weekly','biweekly','monthly']).default('none'), recurrenceCount: z.coerce.number().int().min(1).max(12).default(1) }).superRefine((value, context) => { if (value.mode === 'online' && value.onlineLink && !/^https?:\/\//i.test(value.onlineLink)) context.addIssue({ code: 'custom', path: ['onlineLink'], message: 'Informe um link válido.' }) })
export const appointmentStatusSchema = z.object({ status: z.enum(['scheduled','confirmed','completed','cancelled','no_show']) })
export const paymentSchema = z.object({ patientId: z.string().uuid(), plan: z.string().trim().min(2), amount: z.coerce.number().positive(), dueDate: z.string().min(1), paidOn: z.string().default(''), method: z.string().trim().min(2), status: z.enum(['pending','paid','overdue','cancelled','refunded']), notes: z.string().trim().max(2_000).default('') })
export const paymentStatusSchema = z.object({ status: z.enum(['pending','paid','overdue','cancelled','refunded']) })
export const documentSchema = z.object({ patientId: z.string().uuid(), title: z.string().trim().min(3), type: z.string().trim().min(2), createdOn: z.string().min(1) })
